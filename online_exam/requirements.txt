fastapi==0.115.0          # Web框架，用于构建API
uvicorn[standard]==0.30.0 # ASGI服务器，用于运行FastAPI
sqlalchemy==2.0.35        # ORM框架，操作数据库
pymysql==1.1.1            # MySQL驱动
alembic==1.13.0           # 数据库迁移工具
python-dotenv==1.0.0      # 加载.env环境变量
python-multipart==0.0.12  # 处理表单/文件上传
bcrypt==4.2.0             # 密码哈希加密
pyjwt==2.9.0              # 生成和验证JWT令牌
passlib[bcrypt]==1.7.4    # 密码加密辅助库
pydantic[email]==2.9.0    # 数据校验（FastAPI依赖）
redis==5.0.1              # Redis缓存（可选，用于存储会话）
celery==5.4.0             # 异步任务队列（可选，用于耗时操作）
python-jose==3.3.0        # JWT编码解码